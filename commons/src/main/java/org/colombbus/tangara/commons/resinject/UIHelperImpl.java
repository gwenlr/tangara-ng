package org.colombbus.tangara.commons.resinject;

class UIHelperImpl {

	// public static void injectComponents(Component root) {
	// getResourceMap().injectComponents(root);
	// }

	// public static AttributeSet getAttributeSet(String resource) {
	// Validate.notNull(resource);
	//
	// MutableAttributeSet attributeSet = new SimpleAttributeSet();
	//
	//        String fontResource = resource + ".font";//$NON-NLS-1$
	// loadAttributeSetFont(attributeSet, fontResource);
	//
	//        String underlineResource = resource + ".underline";//$NON-NLS-1$
	// loadAttributeSetUnderline(attributeSet, underlineResource);
	//
	//        String foregroundResource = resource + ".foreground";//$NON-NLS-1$
	// loadAttributeSetForeground(attributeSet, foregroundResource);
	//
	//        String backgroundResource = resource + ".background";//$NON-NLS-1$
	// loadAttributeSetBackground(attributeSet, backgroundResource);
	//
	// // TODO support more style constants
	//
	// return attributeSet;
	// }

	// private static void loadAttributeSetBackground(MutableAttributeSet
	// attributeSet, String key) {
	// if (getResourceMap().containsKey(key)) {
	// Color background = getColor(key);
	// StyleConstants.setBackground(attributeSet, background);
	// }
	// }
	//
	// private static void loadAttributeSetForeground(MutableAttributeSet
	// attributeSet, String key) {
	// if (getResourceMap().containsKey(key)) {
	// Color foreground = getColor(key);
	// StyleConstants.setForeground(attributeSet, foreground);
	// }
	// }
	//
	// private static void loadAttributeSetUnderline(MutableAttributeSet
	// attributeSet, String key) {
	// if (getResourceMap().containsKey(key)) {
	// Boolean underline = getBoolean(key);
	// StyleConstants.setUnderline(attributeSet, underline.booleanValue());
	// }
	// }
	//
	// private static void loadAttributeSetFont(MutableAttributeSet
	// attributeSet, String key) {
	// if (getResourceMap().containsKey(key)) {
	// Font font = getFont(key);
	// String fontFamily = font.getFamily();
	// StyleConstants.setFontFamily(attributeSet, fontFamily);
	// int fontSize = font.getSize();
	// StyleConstants.setFontSize(attributeSet, fontSize);
	// StyleConstants.setBold(attributeSet, font.isBold());
	// StyleConstants.setItalic(attributeSet, font.isItalic());
	// }
	// }

}
